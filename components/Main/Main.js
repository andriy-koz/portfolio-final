import Features from './Features';
import SectionAbout from './SectionAbout';

const Main = () => {
  return (
    <main>
      <SectionAbout />
      <Features />
    </main>
  );
};

export default Main;
